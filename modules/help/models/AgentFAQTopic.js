import moment from "moment";
import Model from "../../../utils/Model";

class AgentFAQTopic extends Model {
    constructor(props) {
        super(props);

        this.initialize(props);
    }

    initialize(props) {
        super.initialize(props);
    }
}

export default AgentFAQTopic;
